@startuml
hide footbox
actor "Admin" as Admin
boundary "DepartmentListPage.jsx" as ListUI
control "api.js" as FrontendAPI
control "departments_api" as API
entity "PhongBan" as DB

Admin -> ListUI: Click "Thêm phòng ban"
activate ListUI
ListUI --> Admin: Hiển thị Modal Thêm mới

Admin -> ListUI: Nhập Tên, Mô tả & Click "Lưu"
ListUI -> FrontendAPI: POST /api/departments/list/
activate FrontendAPI

FrontendAPI -> API: Request tạo phòng ban
activate API

alt Thành công
    API -> DB: Create Department
    activate DB
    DB --> API: Success
    deactivate DB
    
    API --> FrontendAPI: {success: true}
    FrontendAPI --> ListUI: Success
    
    ListUI --> ListUI: Đóng Modal & Reload
    ListUI --> Admin: Thông báo thành công
else Lỗi
    API --> FrontendAPI: {error: "..."}
    FrontendAPI --> ListUI: Error
    ListUI --> Admin: Hiển thị lỗi
end

deactivate API
deactivate FrontendAPI
deactivate ListUI

@enduml
